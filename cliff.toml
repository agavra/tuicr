[changelog]
    ## [{{ version | trcope %}**{{ commit.scope }}:** {% endif %}{{ commit.message | upper_first }}\
    {% endfor %}
{% endfor %}
"""im_start_matches(pat="v") }}]t in commits %}
        - {% if commit.s
footer = ""
trim = true

[git]
conventional_commits = true
filter_un = "^feat", group = "Features" },
    { message = 
header = """
# Changelog

All notable changes to this project will be documented in th
{% if = false
commit_parsers = [
    { message"^fix", group = "Bug Fixes" },
   - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %version %}\conventional = true
split_commits }\
    ## [Unreleased]
{% endif %}\is file.

"""
body = """
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commi  { message = "^doc", group = "Documentation" },
    { message = "^perf", group = "Performance" },
    { message = "^refactor", group = "Refactor" },
    { message = "^style", group = "Styling" },
    { message = "^test", group = "Testing" },
    { message = "^chore\\(release\\)", skip = true },
    { message = "^chore\\(deps\\)", skip = true },
    { message = "^chore", group = "Miscellaneous" },
]
filter_commits = false
tag_pattern = "v[0-9].*"
skip_tags = ""
ignore_tags = ""
topo_order = false
sort_commits = "oldest"
